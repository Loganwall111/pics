import { createRoot } from "react-dom/client";
import { App } from "./App";

/** LUMITAL entry — psychedelic multiverse evolution explorer (v0.1). */
createRoot(document.getElementById("root") ?? document.body).render(<App />);
